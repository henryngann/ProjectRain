import { Button, Grid } from "@mui/material";
import React from "react";

const NavButton = () => {
  return (
    <Grid item>
      <Button>Hello</Button>
    </Grid>
  );
};

export default NavButton;
