import { Typography } from "@mui/material";
import React from "react";

const Header = () => {
  return (
    <Box>
      <Typography> Big Title</Typography>
    </Box>
  );
};

export default Header;
